# dianmeng 无限画布 0.1.54

## Seedance 全模态视频创作正式登场

dianmeng 无限画布现已接入火山方舟官方视频生成能力，覆盖 Seedance 2.0、Seedance 2.0 Fast 与 Seedance 2.5。一个视频节点即可完成文生视频、多参考视频、首帧生成和首尾帧生成，让图片、视频、音频与提示词在无限画布中真正协同创作。

## 核心更新

- 新增火山方舟官方 API 插件，支持异步任务、生成进度、取消、播放、全屏和下载。
- 新增 Seedance 2.5 模型线路，并保留 Seedance 2.0 与 Fast 模型选择。
- 接入火山私域素材库：支持素材组、图片/视频预览、搜索、多选确认与自动刷新临时预览地址。
- 首帧与首尾帧模式现可直接选择素材库图片；确认后显示清晰缩略图，并按首帧、尾帧角色准确提交。
- 统一多模态参考区：连接图片、视频、音频或选择素材库内容后，全部以可视化缩略卡片展示。
- 输入 `@` 可选择 `@图片N`、`@视频N`、`@音频N`；点击素材卡片也能快速插入引用。
- 图片与视频节点的参考缩略图新增悬停删除按钮。删除连线素材会同步断开连接；删除素材库内容会同步移出引用列表，并自动修正提示词中的后续编号。
- 新增视频生成历史：近 7 天的视频结果可悬停播放、全屏查看、下载、再次添加到画布和批量删除。
- 视频生成结果会在任务完成时立即落盘，即使切换画布、节点暂时未渲染或应用关闭，也能在重新进入后恢复。
- 新增 Seedance 提示词优化能力，强化镜头、动作、光线、节奏、声音和素材职责描述。
- 优化视频节点渲染与画布性能，降低多节点场景下的播放器和媒体加载开销。

## 使用提醒

火山方舟模型权限属于账号级能力。若提示 `ModelNotOpen`，请先在当前 API Key 所属的火山方舟账号中开通对应模型或配置可用的推理接入点。
