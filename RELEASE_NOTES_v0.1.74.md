# 点梦无限画布 v0.1.74

本版基于稳定的 v0.1.71 行为继续优化，并修复 v0.1.73 安装包遗漏插件接口的问题。

## 修复

- 恢复画布 Agent 助手及大语言模型提示词功能。
- 修复安装包缺少 `/api/plugins/registry` 导致的 `Unexpected token '<'` 报错。
- 新渠道图片生成会优先使用节点选择的比例和分辨率，并同时兼容多种中转接口字段。
- 大语言模型接口增加连接重置、SSL EOF、429、425 和 5xx 重试，并兼容更多返回文本字段。
- 生成结果 SSE 中断后会读取已保存的任务结果，不会因为网络闪断重复提交。

## 新增与优化

- Agent 可直接读取选中图片、视频关键帧，以及视频节点的上游参考素材。
- Agent 面板打开时可连续点选多个节点，无需按 Ctrl；支持统一取消、缩略图、普通问答、问题诊断和从零撰写。
- 画布内新增显眼的“多开画布”入口，可让不同画布在多个独立窗口同时运行。
- 支持直接把素材上传到火山素材库。
- 新增“自动跟随新增素材”开关，并支持 Delete 键删除节点、连线或素材。
- 火山方舟视频默认总超时保持为 1800 秒。

## 打包自检

- 安装包启动测试现在会同时校验首页和插件注册 JSON 接口，防止再次发布缺路由的安装包。
