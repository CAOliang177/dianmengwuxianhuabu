# dianmeng 无限画布 v0.1.76

本版汇总 v0.1.69 之后的画布、Agent、生成稳定性和桌面端更新。

## 稳定性修复

- 修复旧视频节点缺少 `operation` 字段时触发 React error #185、导致已有大画布无法进入的问题；旧节点只迁移一次，不再循环更新。
- 修复多开画布新窗口触发 React error #185，不同窗口的当前画布相互隔离，可同时运行任务。
- 修复安装包遗漏插件注册接口导致的 `Unexpected token '<'`，并把接口检查加入安装包启动自检。
- 大语言模型接口增加连接重置、SSL EOF、429、425 和 5xx 重试，兼容更多三方接口返回字段。
- 生成结果 SSE 中断后优先读取已保存结果，避免网络闪断造成重复提交。

## Agent 与提示词

- 恢复并优化画布 Agent：支持普通询问、问题诊断、从零撰写及 Seedance 2.0/2.5 分流。
- 面板打开时可连续点选多个图片或视频节点，无需按 Ctrl；支持缩略图、统一取消和读取视频节点上游参考素材。
- 默认预留三方 GPT 兼容接口地址和模型名，只需用户自行填写密钥，安装包不包含任何 API 密钥。

## 画布与生成体验

- 新渠道 Nano Banana / Gemini 图片生成会优先使用节点选择的比例和分辨率，并兼容多种中转接口字段。
- Seedance 2.0/2.5 按用户选择的生成模式优先处理；编辑任务自动使用自适应比例和源视频时长。
- 视频历史“复用提示词”会恢复提示词、生成模式、模型、分辨率、时长和比例，并自动定位原节点。
- 同一句提示词使用不同参数生成时分别保留，不再错误合并。
- 新增“自动跟随新增素材”开关，支持 Delete/Backspace 删除节点、素材或连线。
- 新增显眼的“多开画布”入口，可在独立窗口同时打开不同画布。

## 火山素材库

- 素材库入口和上传按钮常驻显示，支持在客户端选择图片、视频或音频。
- 端内直传采用可选 TOS 临时桶；未配置 TOS 时仍可使用原火山网页素材库，不影响视频生成。
- 火山方舟视频任务默认总超时保持为 1800 秒。

## 验证

- Web 测试 95 项全部通过，插件与图片比例契约测试全部通过。
- Web 与桌面端 TypeScript 类型检查通过。
- 使用本机 52 节点真实画布副本验证，全部节点正常恢复。
