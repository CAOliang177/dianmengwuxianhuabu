# dianmeng无限画布 v0.1.50

本版本以 v0.1.49 为完整基线新增 Seedance 2.5 能力，不会用旧版源码覆盖 v0.1.49 已有的画布保存、生成历史、任务恢复和节点重建逻辑。

## 新增

- 新增“火山方舟”视频 API 插件，默认支持 `doubao-seedance-2-5-260628`，并保留 Seedance 2.0 与 2.0 Fast 选项。
- 视频节点提示词上方新增火山方舟素材库入口，可选择图片、视频和音频素材组，并将素材作为生成参考发送。
- 新增本地 Seedance 2.5 提示词优化工具，按主体动作、场景、视觉风格、镜头剪辑、声音和生成约束整理提示词；优化过程不会把提示词或密钥发送给额外服务。
- 视频比例新增 21:9；Seedance 2.5 支持 4–30 秒、480p/720p、音频生成和 MP4 输出参数。
- 单图图生视频按首帧参考发送；兼容官方与中转站的平铺/嵌套任务响应，并对轮询网络抖动、任务过期和错误下载做了明确处理。
- 自定义方舟接入点可在设置中填写；默认按 Seedance 2.5 请求能力处理，也可选择 2.0 兼容 schema。

## 配置

- 在设置里填写火山方舟 API Key 后即可生成视频。
- 使用素材库时，另行填写火山引擎 Access Key、Secret Key 和区域；安装包不包含任何用户密钥。

## 兼容性

- 保留 v0.1.49 的全部已有图片渠道、模型和本地数据兼容逻辑。
- 只叠加在 v0.1.49 基线上构建，未覆盖画布保存、历史记录或任务恢复代码。
- 本次仅提供安装包，暂不发布在线更新。
