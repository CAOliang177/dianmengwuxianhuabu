# dianmeng 无限画布 0.1.55

## 视频素材工作流，终于完整了

本地视频、火山私域视频素材与 Seedance 多参考生成现已在同一张画布打通。视频可以像图片一样直接上传、拖入、连接、预览和引用，不再需要绕到其他工具处理。

## 核心更新

- 画布右键菜单新增“上传视频”，支持 MP4、MOV、M4V、WebM、AVI 与 MKV。
- 支持从电脑直接拖入一个或多个视频；图片和视频混合拖入时会自动创建对应节点。
- 火山私域素材库新增“全部视频、全部图片、全部音频”快捷入口，可跨素材组搜索和选择。
- 强化素材类型识别：综合读取火山 `AssetType`、媒体类型、文件名和链接，避免视频被误识别为图片。
- 文生视频和多参考视频模式均可直接选择图片、视频与音频素材；选择素材后自动进入多参考工作流。
- 图片、视频、音频素材继续支持缩略图预览、`@图片N` / `@视频N` / `@音频N` 精确引用和悬停删除。
- 首帧与首尾帧模式保持仅选择图片，避免提交不符合 Seedance 模式要求的视频素材。
- 火山素材临时预览地址支持自动刷新，长时间打开画布后仍可继续查看与引用。
- 画布下载保留火山方舟返回的原始视频文件，不进行二次转码或降质。

## 使用方式

1. 在画布空白处右键选择“上传视频”，或直接把视频文件拖入画布。
2. 选择火山方舟视频节点，点击“素材库”。
3. 在素材库顶部选择“全部视频”，即可跨素材组查找视频素材。
4. 选择素材并确认后，可在提示词里使用 `@视频N` 指定素材职责。

## 使用提醒

火山方舟模型和私域素材库权限属于账号级能力。若素材库为空，请检查 Access Key、Secret Key、Region 与 Project Name 是否属于同一个火山账号和项目。
