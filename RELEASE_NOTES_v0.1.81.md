# dianmeng 无限画布 v0.1.81

## Seedance 全能参考模式约束修复

- 强化全能参考的视频职责声明：输入视频只提供主体状态、动作、镜头、节奏和声音参考，不作为待编辑或待延长的源视频。
- 修复方舟仍将全能参考识别为视频编辑，并返回 `ratio must be adaptive`、`duration must be -1` 时任务直接失败的问题。
- 全能参考首次仍严格使用画布选择的比例、时长和分辨率；只有方舟明确返回编辑或延长约束时，客户端才按服务端要求自动纠正参数并重试一次。
- 视频延长误判只将比例切换为 `adaptive`，保留画布选择的时长；视频编辑误判则切换为 `ratio=adaptive`、`duration=-1`，跟随方舟选中的输入视频。
- 自动纠正最多执行一次；其他参数错误不会被吞掉，不会无限重试或重复创建任务。

## 长提示词编辑器

- 修复“编辑文本”窗口在桌面端被公共样式压缩成窄栏的问题，默认提供约 1120px 的宽屏编辑空间。
- 支持一键最大化/还原、清空文本、字数与行数统计。
- 支持 `Ctrl + Enter` 保存并关闭，取消仍会丢弃本次未保存修改。
- 调整正文间距、行高、边框和底部操作区，长提示词阅读与修改更清晰。

## 视频节点预览

- 修复视频节点未选中时使用 `preload=none`，导致生成后一直显示黑屏、必须点击节点才加载画面的问题。
- 视频生成完成后自动预载并解码首帧，加载期间显示明确的画面载入状态。
- 移除固定 `540×304` 的 16:9 预览框；生成前跟随当前选择比例，生成后优先按视频真实宽高比显示。
- 竖屏、方形、横屏和超宽视频会自动调整节点宽高，普通视频素材节点与瀑布流缩略图也同步预载画面。

## Agent 长回答

- 长回答在侧边面板中默认收缩为前 900 字预览，不再把消息区撑到无法操作。
- 超过 900 字或 18 行时显示“展开全文”，点击后打开独立大窗口查看完整内容。
- 完整回答窗口拥有独立纵向滚动、字数统计、复制全文和快捷关闭功能。
- Agent 面板与完整回答窗口均隔离画布滚轮事件，长文本滚动不再触发画布缩放或卡死。
- 输入框限制最大高度并独立滚动，超长提问不会挤占回答区域。

## 验证

- 新增真实视频编辑误判错误的回归测试。
- 新增无关 HTTP 400 不重试测试。
- 新增自动纠正最多一次测试。
- 火山方舟插件 12 项测试全部通过。
- 前端类型检查与桌面端冒烟检查通过。
