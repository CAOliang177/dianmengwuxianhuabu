# dianmeng 无限画布 0.1.56

## Seedance 视频引用稳定性修复

本版本修复画布连接本地视频后，向火山方舟提交超大 Base64 JSON 请求并出现 TLS EOF 的问题。同时把视频任务默认等待时间从 15 分钟延长至 30 分钟。

## 核心更新

- 火山节点检测到本地视频连线时，会在提交前阻止不受支持的请求，并给出明确的素材库操作提示。
- 插件执行层加入同样的保护，批量工作流和其他执行入口不会绕过校验。
- 补齐 MP4、MOV、M4V、AVI、WebM、MKV 等本地视频的 MIME 类型识别，避免视频被标记为图片或未知文件。
- `VOLCENGINE_TIMEOUT` 默认值由 `900` 秒调整为 `1800` 秒。
- 已安装客户端若仍保存旧默认值 `900`，升级后会自动迁移为 `1800`；其他手动设置不会被覆盖。
- 火山素材库的 `asset://` 视频引用保持原有工作方式，不会上传整个视频文件，因此请求更小、更稳定。

## 正确使用参考视频

火山方舟当前官方接口要求参考视频使用公网可访问 URL 或火山素材库资产。桌面端本地文件地址只有本机可访问，不能直接作为远程生成参考。

1. 将视频上传到火山方舟素材库。
2. 在画布的视频生成节点点击“素材库”。
3. 选择“全部视频”或对应素材组中的视频。
4. 确认后使用 `@视频N` 指定参考职责并生成。

本地视频仍可上传或拖入画布，用于预览、整理和连接其他本地工作流；火山 Seedance 参考生成请使用素材库视频。
