# dianmeng 无限画布 0.1.52

## 视频节点重新设计

- 视频生成节点统一为大预览区 + 选中后自动出现的编辑工具条。
- 一个节点内可切换文生视频、多参考视频、首帧/图生视频、首尾帧四种模式。
- 生成结果直接显示在当前视频生成节点内，支持播放、全屏预览和下载。
- 从多张参考图拖线新建视频时，会自动进入多参考视频模式。
- 首尾帧会分别按 `first_frame` 和 `last_frame` 角色发送给火山方舟。

## Seedance 分辨率

- 普通 Seedance 2.0 新增 4K 选择，完整支持 480P、720P、1080P、4K。
- Seedance 2.5 和 Seedance 2.0 Fast 继续只显示其实际支持的 480P、720P，避免无效请求。
- 分辨率选择会随节点一起保存，并进入火山方舟请求体。

## 流畅度优化

- 视频节点不再创建重复的隐藏播放器读取尺寸。
- 画布缩略视频停止鼠标悬停自动播放，减少多节点时的解码和显卡占用。
- 未选中的视频生成节点仅按需加载媒体，降低大量节点同时存在时的资源消耗。
- 参考图订阅只在相关连接或文件变化时刷新，不再因其他节点拖动而整体重绘。
