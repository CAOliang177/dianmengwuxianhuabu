# dianmeng 无限画布 v0.1.78

## Seedance 全能参考容错修复

- 修复全能参考提示词包含“完整复刻参考视频、全程角色或背景替换”等描述时，仍可能被火山方舟识别为视频延长并返回 `ratio must be adaptive` 的问题。
- 全能参考仍优先按画布选择的比例提交；只有方舟明确返回视频延长比例约束时，客户端才自动改用 `ratio=adaptive` 重试，不再直接显示任务失败。
- 自动重试不会修改画布中保存的原始提示词、生成模式、时长、分辨率或素材引用。
- 明确选择 Seedance 2.5 视频编辑时，继续使用 `ratio=adaptive` 与 `duration=-1`，跟随源视频。

## 验证

- 新增方舟服务端将复杂全能参考误判为视频延长的回归测试。
- 火山插件测试、Web 测试和桌面端打包自检通过后发布。
