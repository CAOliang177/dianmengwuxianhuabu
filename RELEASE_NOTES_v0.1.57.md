# dianmeng 无限画布 0.1.57

## Seedance 2.5 专用视频编辑模式

本版本重做视频节点的模式选择体验，并为 Seedance 2.5 增加独立的视频编辑请求链路，修复编辑任务被方舟拒绝为 `ratio`、`duration` 参数不合法的问题。

## 核心更新

- 视频模式改为可视化菜单：文生视频、全能参考、图生视频、首尾帧、图片参考、视频编辑、超长视频。
- “视频编辑”和“超长视频”会根据当前模型自动判断是否可用，并在不可用时直接显示原因。
- 视频编辑仅允许选择一个源视频，界面和插件执行层都会拦截 0 个或多个源视频的错误请求。
- 编辑模式不再显示可误操作的比例与时长控件；输出比例、时长明确标记为跟随源视频。
- 向火山方舟提交编辑任务时自动使用 `ratio=adaptive`、`duration=-1`，符合 Seedance 2.5 编辑任务要求。
- 源视频要求为 4–30 秒。画布会明确提示从火山素材库选择视频，避免再次提交方舟无法读取的桌面本地文件。
- “图片参考”与“全能参考”分离，前者只接收图片，后者可混合图片、视频和音频。
- “超长视频”作为 Seedance 2.5 Beta 模式提供，切换后默认使用 30 秒时长。
- 修复模式菜单在靠近画布底部时被工具栏遮挡的问题。

## 三方 GPT 提示词大模型插件

- 新增“提示词大模型（OpenAI 兼容）”插件，可配置三方 API Base URL、API Key 和模型 ID。
- 兼容常见的 `POST /v1/chat/completions` 协议，也允许直接填写完整的 `/chat/completions` 地址。
- Seedance 提示词优化改为由大语言模型理解创意、素材职责和编辑模式后重写，不再直接套用本地通用模板。
- 创意 Skill 会把方法、约束和用户当前想法一起交给模型推理，只返回可直接用于生成的最终提示词。
- 模型调用失败会显示真实错误，不会静默退回本地词库，便于定位接口地址、模型 ID 或额度问题。

## 提示词大模型配置

在设置的插件列表中找到“提示词大模型（OpenAI 兼容）”，至少填写：

- `PROMPT_LLM_BASE_URL`：三方接口根地址，通常以 `/v1` 结尾。
- `PROMPT_LLM_API_KEY`：三方接口密钥。
- `PROMPT_LLM_MODEL`：服务商实际支持的模型 ID。

如服务商不接受 `max_tokens`、不支持 `system` 角色，可继续调整插件中的 token 参数名和指令角色兼容项。

## 视频编辑使用方法

1. 在视频生成节点中选择火山方舟插件和 Seedance 2.5 模型。
2. 点击“素材库”，选择一个 4–30 秒的视频资产。
3. 打开模式菜单，选择“视频编辑”。
4. 输入需要修改、替换或延展的内容后生成。

视频编辑的画幅和时长由输入视频决定，无需手动设置。桌面端本地视频仍不能直接提交给远程方舟接口；请先将其上传为火山素材库资产。
