# 点梦无限画布 v0.1.73

## 本次更新

- 修复新渠道 Nano Banana / Gemini 图片生成中，所选宽高比和分辨率可能不生效的问题；请求会同时发送标准参数与兼容参数，并加入明确的画布约束。
- 火山素材库支持在端内直接上传图片、视频和音频。文件先暂存到火山 TOS，再创建方舟素材并等待处理完成。
- 新增“自动跟随新节点”开关，可随时关闭上传素材或新增节点后的自动定位、缩放。
- 支持使用 `Delete` 或 `Backspace` 删除当前选中的节点、素材节点或连线。
- 历史画布支持“在新窗口打开”，多个画布可同时打开并分别运行任务。

## 火山素材直传配置

除已有火山方舟凭证外，需要在设置中填写 `VOLCENGINE_TOS_BUCKET`。如使用自定义 TOS 接入点，再填写 `VOLCENGINE_TOS_ENDPOINT`；访问密钥默认复用火山素材库的 AK/SK，也可单独配置 TOS AK/SK。

## 验证

- TypeScript 类型检查通过（Web 与桌面端）。
- 新渠道图片比例/分辨率契约测试通过。
- 火山素材库接口测试通过。
- TOS SDK 签名运行时检查通过。
